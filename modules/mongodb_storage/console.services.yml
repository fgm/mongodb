services:
  mongodb_storage.command.sql_import:
    class: Drupal\mongodb_storage\Command\SqlImportCommand
    arguments:
      - '@mongodb.storage.sql_import'
    tags:
      - { name: drupal.command }
